{
  "name": "api-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "openapi-generator generate -i reference/D-Tools-SI-API.v1.json -g typescript-fetch -c generate.json -o ./dtapi/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^1.0.12-4.3.0"
  }
}
